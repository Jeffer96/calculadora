<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/tema0</item>
        <item name="colorPrimaryDark">@color/tema1</item>
        <item name="colorAccent">@color/font1</item>
    </style>

    <style name="buttonCacl">
        <item name="android:layout_width"> 100dp </item>
        <item name="android:background">@color/tema1</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/font2</item>
    </style>

</resources>
